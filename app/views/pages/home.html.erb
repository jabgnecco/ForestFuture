<h1>Welcome to the Home Page!</h1>

<% if @projects.any? %>
  <h2>Projects:</h2>
  <ul>
    <% @projects.each do |project| %>
      <li><%= project.name %></li>
    <% end %>
  </ul>
<% else %>
  <p>No projects available.</p>
<% end %>
